# todoEdidAddIfNotPresent

insert dialoge to ask what to add
    list containing
    recordtypes
    and rawtext
    if record selected
    then chose
    from current record
      from master
      from other
      select from list
